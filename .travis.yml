language: node_js
node_js: 
  - stable
branches:
 only:
  - develop

script:
  - npm i
  - npm run test
cache:
  directories:
    - node_modules
deploy:
  local_dir: src
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: develop